/**
 * usePollStatus hook — polls /api/v1/github/status every 5s.
 * Stub — will be implemented in Week 6.
 */
export function usePollStatus(runId) {
  return {
    status: null,
    loading: false,
    error: null,
  }
}
