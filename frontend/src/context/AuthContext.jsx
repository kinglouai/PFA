/**
 * AuthContext — GitHub OAuth token + user info.
 * Stub — will be implemented in Week 5.
 */
import { createContext } from 'react'

const AuthContext = createContext(null)

export default AuthContext
